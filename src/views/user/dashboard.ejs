<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - VPS Control Panel
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/sidebar.css">
</head>

<body>
    <%- include('../components/sidebar') %>

        <div class="main-content">
            <%- include('../components/header') %>

                <div class="content-wrapper">
                    <div class="page-header">
                        <h1 class="page-title">Welcome back, <%= user.name %>!</h1>
                        <p class="page-subtitle">Manage your virtual private servers</p>
                    </div>

                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-label">Total Servers</div>
                            <div class="stat-value">
                                <%= stats.totalServers %>
                            </div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Active Servers</div>
                            <div class="stat-value">
                                <%= stats.activeServers %>
                            </div>
                            <div class="stat-change positive">Running</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Total CPU Cores</div>
                            <div class="stat-value">
                                <%= totalResources.cpu %>
                            </div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Total RAM</div>
                            <div class="stat-value">
                                <%= (totalResources.ram / 1024).toFixed(1) %> GB
                            </div>
                        </div>
                    </div>

                    <div class="card">
                        <div class="flex-between" style="margin-bottom: 1.5rem;">
                            <h3 style="font-size: 1.125rem;">Your Servers</h3>
                            <a href="/user/servers" class="btn btn-primary">View All</a>
                        </div>

                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>IP Address</th>
                                        <th>Location</th>
                                        <th>Plan</th>
                                        <th>Status</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (servers.length===0) { %>
                                        <tr>
                                            <td colspan="6" class="text-center text-muted">
                                                No servers yet. Contact administrator to deploy your first server.
                                            </td>
                                        </tr>
                                        <% } else { %>
                                            <% servers.slice(0, 5).forEach(server=> { %>
                                                <tr>
                                                    <td><strong>
                                                            <%= server.name %>
                                                        </strong></td>
                                                    <td>
                                                        <%= server.ip_address?.address || 'N/A' %>
                                                    </td>
                                                    <td>
                                                        <%= server.node.location.name %>
                                                    </td>
                                                    <td>
                                                        <%= server.plan.name %>
                                                    </td>
                                                    <td>
                                                        <% if (server.status==='active' ) { %>
                                                            <span class="badge badge-success">Active</span>
                                                            <% } else if (server.status==='suspended' ) { %>
                                                                <span class="badge badge-error">Suspended</span>
                                                                <% } else { %>
                                                                    <span class="badge badge-info">
                                                                        <%= server.status %>
                                                                    </span>
                                                                    <% } %>
                                                    </td>
                                                    <td>
                                                        <a href="/user/servers/<%= server.id %>"
                                                            class="btn btn-secondary"
                                                            style="font-size: 0.75rem; padding: 0.375rem 0.75rem;">
                                                            Manage
                                                        </a>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
        </div>
</body>

</html>