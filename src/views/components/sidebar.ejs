<div class="sidebar">
    <div class="sidebar-brand">
        <div class="sidebar-brand-text">VPS Panel</div>
    </div>

    <nav class="sidebar-nav">
        <% if (isAdmin) { %>
            <!-- Admin Navigation -->
            <div class="nav-section">
                <div class="nav-section-title">Administration</div>
                <a href="/admin/dashboard" class="nav-item <%= title.includes('Dashboard') ? 'active' : '' %>">
                    <span class="nav-icon">📊</span>
                    Dashboard
                </a>
                <a href="/admin/locations" class="nav-item <%= title.includes('Locations') ? 'active' : '' %>">
                    <span class="nav-icon">📍</span>
                    Locations
                </a>
                <a href="/admin/nodes" class="nav-item <%= title.includes('Nodes') ? 'active' : '' %>">
                    <span class="nav-icon">🖥️</span>
                    Nodes
                </a>
                <a href="/admin/servers"
                    class="nav-item <%= title.includes('Server') && !title.includes('User') ? 'active' : '' %>">
                    <span class="nav-icon">💻</span>
                    Servers
                </a>
                <a href="/admin/ipam" class="nav-item <%= title.includes('IP') ? 'active' : '' %>">
                    <span class="nav-icon">🌐</span>
                    IPAM
                </a>
            </div>

            <div class="nav-section">
                <div class="nav-section-title">Management</div>
                <a href="/admin/users" class="nav-item <%= title.includes('Users') ? 'active' : '' %>">
                    <span class="nav-icon">👥</span>
                    Users
                </a>
                <a href="/admin/plans" class="nav-item <%= title.includes('Plans') ? 'active' : '' %>">
                    <span class="nav-icon">📦</span>
                    Plans
                </a>
                <a href="/admin/api-tokens" class="nav-item <%= title.includes('API') ? 'active' : '' %>">
                    <span class="nav-icon">🔑</span>
                    API Tokens
                </a>
            </div>
            <% } else { %>
                <!-- User Navigation -->
                <div class="nav-section">
                    <div class="nav-section-title">My Services</div>
                    <a href="/user/dashboard" class="nav-item <%= title.includes('Dashboard') ? 'active' : '' %>">
                        <span class="nav-icon">📊</span>
                        Dashboard
                    </a>
                    <a href="/user/servers"
                        class="nav-item <%= title.includes('Servers') || title.includes('Server') ? 'active' : '' %>">
                        <span class="nav-icon">💻</span>
                        My Servers
                    </a>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Account</div>
                    <a href="/user/profile" class="nav-item <%= title.includes('Profile') ? 'active' : '' %>">
                        <span class="nav-icon">⚙️</span>
                        Profile
                    </a>
                </div>
                <% } %>
    </nav>

    <div class="sidebar-footer">
        <div class="sidebar-user">
            <div class="sidebar-user-avatar">
                <%= user.name.charAt(0).toUpperCase() %>
            </div>
            <div class="sidebar-user-info">
                <div class="sidebar-user-name">
                    <%= user.name %>
                </div>
                <div class="sidebar-user-role">
                    <%= isAdmin ? 'Administrator' : 'User' %>
                </div>
            </div>
        </div>
        <form action="/auth/logout" method="POST">
            <button type="submit" class="btn btn-secondary" style="width: 100%;">
                Logout
            </button>
        </form>
    </div>
</div>